# SpotifyMonthlyPlaylists
Automatically add newly saved songs to a monthly playlist using Spotify's API.


Usage:

Add your Spotify API keys to main.py.
```
$ pip install -r requirements.txt
$ python ./main.py 
```
